services:
  web:
    build: .
    ports:
      - "80:5000"
    depends_on:
      - db
    restart: always

  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: jobnest
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: